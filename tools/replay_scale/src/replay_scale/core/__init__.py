"""Pure computation layer: SE(3) math, data model, scale estimation, odom sync.

Nothing in this package reads or writes files, parses configuration or prints.
It may not import from any other layer of ``replay_scale``.
"""
