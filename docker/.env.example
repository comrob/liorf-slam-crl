# User Configuration
USER_UID=1000
USER_GID=1000

# Paths
HOST_BAGS_PATH=~/bag/carles_squere/final_jazzy_mcap_filtered
HOST_MAPS_PATH=/home/seva/Projects/OPSEC/Charles_GPS

# Zenoh configuration (optional override)
# Leave empty to use docker/zenoh.json5 (recommended)
# Set to custom path to use your own config:
# HOST_ZENOH_CONFIG=~/zenoh.json5
HOST_ZENOH_CONFIG=

# Note: The 'run_slam' compose profile is optional and used only for
# local benchmark compatibility testing. External evaluation frameworks
# use their own orchestration and only consume the built image.

# Default bagfile to play (must exist inside HOST_BAGS_PATH)
DEFAULT_BAG_FILENAME=final_jazzy_mcap_filtered_0.mcap
BAG_FILENAME=final_jazzy_mcap_filtered_0.mcap

# GPU
DISPLAY=:0